import Root from "./markdown-editor.svelte";

export type {
   MarkdownEditorPreviewHandler,
   MarkdownEditorProps,
   MarkdownEditorUploadContext,
   MarkdownEditorUploadHandler,
} from "./markdown-editor.shared.js";

export {
   Root,
   //
   Root as MarkdownEditor,
};
