import type { StepperProps, StepperStep } from "./stepper.svelte";
import Root from "./stepper.svelte";

export {
   Root,
   type StepperProps as Props,
   Root as Stepper,
   type StepperProps,
   type StepperStep,
};
