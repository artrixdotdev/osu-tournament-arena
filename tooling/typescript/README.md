# @ota/tsconfig

Shared TypeScript configurations for the monorepo.

## Usage

Extend in your `tsconfig.json`:

```json
{
   "extends": "@ota/tsconfig/base.json"
}
```

Available configs are in the package root as JSON files.
